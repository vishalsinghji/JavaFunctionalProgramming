package Spliterator;

import java.util.Spliterator;

public class Intro {
//    Spliterator is an interface we can implement this stream on custom source.
//    It's an object to access the data which an stream can use.
public static void main(String[] args) {
//    boolean tryAdvance(Consumer<? super T> var1); -> If a remaining elements present it performs give action on it.
//    Spliterator<T> trySplit(); -> It is used for parallel stream. (Balance stream in Jdk for high performance)

//    long estimateSize(); -> It return estimate of no elements in for each traversal.
//    int characteristics(); -> It returns a set of characteristics of a spliterator
}

}

